[project]
name = "manuals-artifact-engine"
version = "2.0.0"
description = "Domain-agnostic JSON-driven artifact generation engine"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
